You are a **senior full-stack engineer**.

Fix and rebuild the **Dashboard Home Page** of my application so it works correctly with the backend APIs.

Tech stack:

Frontend

* React
* Vite
* TailwindCSS
* Zustand
* Axios

Backend

* FastAPI
* MongoDB
* Beanie ODM

The home page must display **user activity, suggestions, nearby users, and login streak** and must connect correctly to backend APIs.

---

# FRONTEND TASKS

Create or fix the component:

```
frontend/src/pages/dashboard/HomePage.jsx
```

The Home Page must include the following sections:

• Welcome header
• Search bar
• Login streak section
• Nearby people section
• Today's match suggestion
• Activity summary

All sections must load data from backend APIs.

---

# 1. Welcome Section

Display the logged-in user's name.

Example:

```
Welcome back, {user.name}
```

Get the user from Zustand:

```
authStore
```

---

# 2. Search Bar

Allow searching users.

When typing in the search bar:

```
GET /api/users/search?q=query
```

Show results in a dropdown list.

Each result should show:

* profile picture
* name

Clicking a result should navigate to:

```
/dashboard/profile/{userId}
```

---

# 3. Login Streak Section

Fetch login streak from:

```
GET /api/users/streak
```

Example response:

```
{
 "streak_days": 3,
 "next_reward": 5
}
```

Display:

* streak days
* reward coins
* small streak progress UI

Example text:

```
🔥 3 Day Streak
Come back tomorrow to earn 5 coins
```

---

# 4. Nearby People Section

Fetch nearby users:

```
GET /api/users/nearby
```

Example response:

```
{
 "users":[
  {
   "id":"...",
   "name":"...",
   "profile_picture":"...",
   "is_online":true
  }
 ]
}
```

Display users in a responsive grid.

Each user card must show:

• profile picture
• name
• online indicator

Click card → navigate:

```
/dashboard/profile/{id}
```

---

# 5. Today's Match Section

Fetch suggestions from:

```
GET /api/users/suggestions
```

Display 1–3 recommended users.

Each card should include:

• profile image
• name
• short bio (if available)

---

# 6. Activity Summary Section

Show activity cards:

• Messages sent
• Matches
• Coins balance
• Profile views

Fetch data from:

```
GET /api/users/dashboard-stats
```

Example response:

```
{
 "messages_sent":10,
 "matches":4,
 "coins":25,
 "profile_views":12
}
```

Display cards in a grid.

---

# FRONTEND PERFORMANCE RULES

Follow these rules:

• Use `useEffect` for data loading
• Do not duplicate API calls
• Show loading skeleton while fetching
• Handle errors gracefully
• Use Axios API service

Example:

```
const res = await userAPI.getNearby()
setNearbyUsers(res.data.users)
```

---

# UI REQUIREMENTS

The Home Page must have:

• clean modern layout
• Tailwind responsive design
• smooth hover animations
• loading states
• empty states if no data

Example layout order:

```
Welcome Header
Search Bar
Login Streak
Nearby Users
Today's Match
Activity Summary
```

---

# BACKEND TASKS

Ensure the following endpoints exist and return valid data:

```
GET /api/users/nearby
GET /api/users/suggestions
GET /api/users/streak
GET /api/users/dashboard-stats
GET /api/users/search
```

All endpoints must return HTTP 200 and valid JSON.

Example streak endpoint:

```
GET /api/users/streak

{
 "streak_days":3,
 "next_reward":5
}
```

---

# GOAL

The Home Page must:

• load quickly
• fetch data correctly from backend
• show nearby users
• display activity statistics
• allow searching users
• show login streak
• work smoothly with no console errors

The page should behave like a modern **dating app dashboard**.
